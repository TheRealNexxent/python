s = {} #Empty dictionary, not empty set

x = set() #Empty Set

# however rest all set willl be using same denominations
y = {1, 1, 1, 1, 2, 3, 4, 5} #Set with values. Cannot have repeated elements. will return 1 once 1 time.
print(y)
